#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    if idx < 0:
        return list
    if idx >= len(list):
        return list
    else:
        list[idx] = element
        return list
